<?php

$displayErrorDetails = TRUE; // set to false in production
$addContentLengthHeader = FALSE; // allow the web server to send the content-length header

$renderer = array(
	'template_path' => (DIR_ROOT . '/public/'),
);
